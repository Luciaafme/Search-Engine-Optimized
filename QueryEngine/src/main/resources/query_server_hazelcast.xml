<hazelcast xmlns="http://www.hazelcast.com/schema/config"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:schemaLocation="http://www.hazelcast.com/schema/config
           https://hazelcast.com/schema/config/hazelcast-config-3.12.13.xsd">
    <group>
        <name>BookCluster</name>
        <password>password123</password>
    </group>

    <network>
        <!-- Configuración del puerto -->
        <port auto-increment="true" port-count="100">5801</port>

        <!-- Configuración de interfaces -->
        <interfaces enabled="true">
            <interface>10.26.14.*</interface> <!-- Asegúrate de permitir la subred relevante -->
            <interface>172.17.0.*</interface> <!-- Red interna de Docker -->
        </interfaces>

        <!-- Configuración de descubrimiento de nodos -->
        <join>
            <tcp-ip enabled="true">
                <!-- Lista de miembros en el clúster -->
                <member>10.26.14.207</member>
                <member>10.26.14.203</member>
                <member>10.26.14.208</member>
            </tcp-ip>
            <multicast enabled="false"/>
        </join>

        <!-- Configuración de la red -->
        <public-address>10.26.14.207:5801</public-address> <!-- Dirección pública específica del nodo -->
    </network>

    <!-- Configuración de mapas -->
    <map name="datalakeMap">
        <backup-count>1</backup-count>
        <time-to-live-seconds>0</time-to-live-seconds>
    </map>

    <map name="wordDatamartMap">
        <backup-count>1</backup-count>
        <time-to-live-seconds>0</time-to-live-seconds>
    </map>

    <map name="metadataDatamartMap">
        <backup-count>1</backup-count>
        <time-to-live-seconds>0</time-to-live-seconds>
    </map>
</hazelcast>
